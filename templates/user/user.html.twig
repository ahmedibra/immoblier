{% extends 'base.html.twig' %}
{% block title %}Admin{% endblock %}

{% block body %}
<section class="contact-section">
            <div class="container">
 
                <div class="row">
                <table>
                <tr><td><a href="{{ path('annonce_new') }}" class="btn btn-info btn-xs pull-right mb-10 mt-10">Ajouter annonce</a></td>
                </table>
    <div class="row col-md-12 col-md-offset-2 custyle">
  
    <table class="table table-striped custab">
    <thead>
    <hr>
        <tr>
            <th>Titre</th>
            <th>Créer le</th>
            <th class="text-center">Action</th>
        </tr>
    </thead>
    {% for annonce in annonces %}
            <tr>
                <td>{{annonce.titre}}</td>
                <td>{{annonce.createdAt | date('d-m-y')}} à {{annonce.createdAt | date('H:i')}}</td>
                <td class="text-center"><a class='btn btn-info btn-xs' href="{{ path('annonce_user-edit', {'id' : annonce.id} ) }}">
                <span class="glyphicon glyphicon-edit"></span> Modifier</a> <a href="{{ path('annonce_user-delete', {'id' : annonce.id} ) }}"  class="btn btn-danger btn-xs">
                <span class="glyphicon glyphicon-remove"></span> Supprimer</a></td>
                
                
            </tr>
            {% endfor %}
    </table>
    {# display navigation #}
<div class="container navigation mt-5">
    {{ knp_pagination_render(annonces) }}
</div>
    </div>
</div>
</div></div>
</section>
{% endblock %}